# punto34.py

# Definimos una lista de números
numeros = [0, 0, 1, 0]

# Verificamos si al menos un elemento es distinto de cero usando any()
if any(numeros):
    print("Al menos un número es no cero.")