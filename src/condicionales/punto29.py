# Definimos el dividendo
dividendo = 10
# Definimos el divisor
divisor = 0

# Usamos una expresión condicional para evitar división por cero
resultado = dividendo / divisor if divisor != 0 else "División por cero no permitida"

# Imprimimos el resultado
print(resultado)