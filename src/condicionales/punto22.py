# punto22.py

# Definimos el usuario
usuario = "admin"
# Definimos la contraseña
contrasena = "1234"

# Verificamos si el usuario es "admin"
if usuario == "admin":
    # Si el usuario es "admin", verificamos la contraseña
    if contrasena == "1234":
        # Si la contraseña coincide, se concede el acceso
        print("Acceso concedido.")