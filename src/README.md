**CONDICIONALES**

# Punto1

Este programa revisa si una persona es mayor de edad.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/condicionales` y ejecutar:

python punto1.py

# Punto2

Este programa revisa la temperatura y dice si hace calor.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.
Ejecutar:
   python punto2.py

# Punto3

Este programa verifica la hora del dia y dice buenos dias.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.
Ejecutar:
   python punto3.py

# Punto4

Este programa verifica si la persona tiene edad suficiente para votar.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto4.py

# Punto5

Este programa verifica si la persona ingreso la contraseña correcta o no y se le da el acceso.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto5.py

# Punto6

Este programa verifica si un numero es par o impar por medio de un operario aritmetico.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto6.py

# Punto7

Este programa simula un cajero automático que permite realizar un retiro de dinero verificando si el saldo es suficiente.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto7.py

# Punto8

Este programa determina si un número es positivo, negativo o cero.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto8.py

# Punto9

Este programa evalúa una nota numérica y muestra la calificación correspondiente en forma de texto (Sobresaliente, Notable, Aprobado o Suspenso).

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto9.py

# Punto10

Este programa evalúa la edad de una persona y determina si es menor de edad, adulto o mayor de 65 años.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto10.py

# Punto11

Este programa evalúa el valor de una variable `color` y muestra un mensaje dependiendo de si es rojo, verde o azul.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto11.py

# Punto12

Este programa pide al usuario que ingrese el nombre de una fruta y muestra un mensaje dependiendo de si la fruta es manzana, naranja o plátano. Si la fruta ingresada no coincide con ninguna de estas, se mostrará "Fruta desconocida".

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto12.py

# Punto13

Este programa evalúa las coordenadas de un punto `(x, y)` y muestra un mensaje indicando si el punto está en el origen, en el eje X, en el eje Y, o en cualquier otra coordenada.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto13.py

# Punto14

Este programa evalúa la edad de una persona y muestra un mensaje indicando si es menor de edad, adulto o adulto mayor.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto14.py

# Punto15

Este programa recorre una lista de usuarios y evalúa el rol de cada uno usando `match`. Dependiendo del rol, muestra un mensaje indicando los permisos o tipo de usuario.  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto15.py

# Punto16

Este programa revisa cómo está formada una lista de números usando `match`.  
Dependiendo de cuántos elementos tenga, muestra un mensaje diferente.  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto16.py

# Punto17

Este programa verifica si una persona puede obtener un crédito.  
La persona debe cumplir dos condiciones Si cumple ambas, muestra el mensaje: "Eres elegible para el crédito."  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto17.py

# Punto18

Este programa revisa si un día de la semana es fin de semana.  
Si el día es sábado o domingo, muestra el mensaje: "Es fin de semana."  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto18.py

# Punto19

Este programa revisa si está lloviendo.  
Si no llueve, muestra el mensaje: "Podemos salir al parque."  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto19.py

# Punto20

Este programa revisa si una persona puede obtener la licencia de conducir
Si cumple alguna de las condiciones, muestra: "Puedes obtener la licencia de conducir."  
Si no, muestra: "No cumples los requisitos para la licencia."  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto20.py

# Punto21

Este programa trabaja con valores booleanos y operadores lógicos.  
Calcula el resultado de la expresión `a or b and c` y lo muestra en pantalla.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto21.py

# Punto22

Este programa verifica si el usuario y la contraseña son correctos.  
Si el usuario es "admin" y la contraseña es "1234", muestra "Acceso concedido".  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto22.py

# Punto23

Este programa evalúa la edad y el estado civil de una persona.  
Muestra un mensaje indicando si es menor de edad, adulto soltero o adulto casado.  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto23.py

# Punto24

Este programa evalúa la edad de una persona y si tiene permiso de los padres.  
Muestra un mensaje indicando si puede obtener la licencia de conducir, necesita permiso de los padres, o es demasiado joven para conducir.  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto24.py

# Punto25

Este programa compara tres números y muestra cuál es el mayor entre ellos.  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto25.py

# Punto26

Este programa evalúa la edad de una persona y muestra un mensaje indicando si es mayor o menor de edad, usando una expresión condicional (ternaria).  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto26.py

# Punto27

Este programa compara dos números y muestra cuál es el mayor usando una expresión condicional (ternaria).  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto27.py

# Punto28

Este programa evalúa la edad de una persona y asigna una categoría:"Menor" si tiene menos de 18 años,"Joven Adulto" si tiene entre 18 y 29 años y "Adulto" si tiene 30 años o más  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto28.py

# Punto29

Este programa realiza una división y evita el error de división por cero.  
Si el divisor es distinto de cero, realiza la división; si no, muestra un mensaje indicando que no está permitida la división por cero.  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto29.py

# Punto30

Este programa verifica si una lista tiene elementos y si el primer elemento es `'Python'`.  
Si se cumplen ambas condiciones, muestra un mensaje indicando que el primer elemento es `'Python'`.  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto30.py

# Punto32

Este programa verifica si un acceso está permitido o registrado.  
Si alguna de las condiciones se cumple, muestra el mensaje "Acceso concedido".  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto32.py

# Punto33

Este programa verifica si un acceso está permitido o, en su defecto, registrado.  
Si alguna de las condiciones se cumple, muestra el mensaje "Acceso concedido".  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto33.py

# Punto34

Este programa verifica si en una lista hay al menos un número distinto de cero.  
Si se cumple la condición, muestra el mensaje "Al menos un número es no cero".  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto34.py

# Punto35

Este programa verifica si todas las condiciones de una lista son verdaderas.  
Si todas lo son, muestra "Todas las condiciones son verdaderas"; si no, muestra "Al menos una condición es falsa".  

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/condicionales`.  
Ejecutar:
   python punto35.py

**INTERACTIVOS**

# Punto1

Este programa recorre una lista de frutas con un ciclo for y muestra cada una en pantalla.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`. 
Ejecutar:
   python punto1.py

# Punto2

Este programa enseña cómo usar un ciclo for con range(5) para mostrar los números del 0 al 4.
En cada vuelta, se imprime el número que está en la posición del ciclo.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`. 
Ejecutar:
   python punto2.py

## Punto3

Este programa guarda una lista con tres nombres y muestra cada uno junto con su posición en la lista.
Se usa un for con range(len(lista)) para recorrer los índices de la lista y acceder a cada elemento.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.
Ejecutar:
   python punto3.py

## Punto4

Este programa guarda una lista con tres nombres y los muestra junto con su posición en la lista.
En este caso se usa enumerate en lugar de range(len(lista)), lo que simplifica la obtención del índice y el valor al mismo tiempo.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.
Ejecutar:
   python punto4.py

## Punto5

Este programa guarda la palabra "Python" en una variable y luego recorre cada letra con un bucle for.
En cada iteración, el programa muestra una letra en pantalla.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.
Ejecutar:
   python punto5.py

## Punto6

Este programa guarda la información de un usuario dentro de un diccionario en Python.
Después, recorre todas las claves del diccionario con un bucle for, mostrando la clave y su valor correspondiente en pantalla.

## Cómo ejecutarlo

Abrir la terminal en la carpeta src/iterativas.
Ejecutar:
   python punto6.py

## Punto7

Este programa utiliza comprensión de listas en Python.  
Primero, crea una lista con los cuadrados de los números del 1 al 5.  
Después, genera una lista con los números pares del 0 al 9, usando una condición dentro de la comprensión de listas.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta src/iterativas.  
Ejecutar:
   python punto7.py

## Punto8

Este programa crea una matriz de multiplicación 3x3 en Python.  
Usa dos bucles `for` anidados: el primero recorre las filas y el segundo las columnas.  
En cada iteración muestra la multiplicación de los valores correspondientes, formando una tabla con los resultados.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta src/iterativas.  
Ejecutar:
   python punto8.py

## Punto9

Este programa calcula la suma de los primeros `n` números enteros en Python, 
utilizando un bucle `for`. Finalmente, muestra el resultado en pantalla.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto9.py

# Punto10

Este programa calcula la suma de los números pares que existen entre `1` y un número `n` que ingresa el usuario.  
Se utiliza un ciclo `for` y una condición `if` para verificar cuáles son pares y sumarlos.  
Al final, se muestra el resultado total en pantalla.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/iterativas`.  
   python punto10.py

## Punto11

Este programa analiza las temperaturas de una semana.  
Primero identifica cuál fue el día más caluroso, luego calcula la temperatura promedio de todos los días,  
y finalmente muestra qué días estuvieron por encima del promedio.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar el siguiente comando:
   python punto11.py

## Punto12

Este programa muestra cómo funciona un ciclo `while` en Python.  
El ciclo repite un bloque de instrucciones mientras la condición sea verdadera.  

En el ejemplo, se utiliza un contador que va aumentando en cada vuelta hasta que deja de cumplirse la condición.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto12.py

## Punto13

Este programa pide al usuario que introduzca un número.  
Mientras la entrada no sea un número válido, seguirá repitiendo la pregunta.  
Cuando el usuario finalmente introduce un número, el programa lo muestra en pantalla.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto13.py

## Punto14

Este programa es un juego de adivinanza de números en Python.
El sistema elige un número aleatorio entre 1 y 10, y el jugador tiene hasta 3 intentos para adivinarlo.
Después de cada intento, se da una pista indicando si el número secreto es mayor o menor.
Si no se logra adivinar en los intentos permitidos, el programa revela el número.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto14.py

## Punto15

Este programa simula un sistema de gastos con un saldo inicial de 1000€.  
El usuario puede introducir diferentes cantidades a gastar 

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:  
   python punto15.py

## Punto16

Este programa solicita al usuario si desea continuar o no.  
El usuario debe responder con 's' para continuar o 'n' para finalizar.  
Si se ingresa un valor diferente, el programa indica que la respuesta no es válida.  
El ciclo se repite hasta que el usuario responde con 'n', momento en el cual el programa termina.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto16.py

## Punto17

Este programa calcula el factorial de un número en Python.  
Se utiliza una función llamada `calcular_factorial` que multiplica todos los enteros desde 1 hasta el número ingresado usando un bucle `while`.  
Finalmente, muestra el resultado en pantalla.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto17.py

## Punto18

Este programa calcula la raíz cuadrada de un número en Python utilizando el método de aproximaciones (Newton-Raphson).  
Se define una función `calcular_raiz_cuadrada` que ajusta la aproximación hasta que el cuadrado del valor estimado esté dentro de una precisión deseada.  
Finalmente, muestra el resultado en pantalla con seis decimales.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto18.py

## Punto19

Este programa solicita al usuario que ingrese un número dentro de un rango específico en Python.  
Se define la función `obtener_numero_en_rango`, que repite la solicitud hasta que el valor ingresado sea un número entero y esté dentro del rango indicado.  
Finalmente, muestra en pantalla el número válido introducido por el usuario.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto19.py

## Punto20

Este programa imprime un triángulo de asteriscos en Python utilizando un bucle `while`.  
Se define la función `imprimir_triangulo` que, fila por fila, imprime la cantidad de asteriscos correspondiente a la altura indicada.  
Finalmente, se llama a la función con la altura deseada para mostrar el triángulo en pantalla.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto20.py

## Punto21

Este programa muestra cómo sumar los números del 1 al 10 en Python usando dos tipos de bucles: `while` y `for`.  
Se comparan ambas versiones mostrando el mismo resultado en pantalla.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto21.py

## Punto22

Este programa recorre los números del 1 al 10 en Python usando un bucle `for`.  
Cuando encuentra el número 5, muestra un mensaje y termina el bucle usando `break`.  
Después de salir del bucle, muestra un mensaje indicando que el bucle ha terminado.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto22.py

## Punto23

Este programa busca un elemento en una lista en Python.  
Se define la función `buscar_elemento` que recorre la lista usando `enumerate` y devuelve la posición del elemento buscado.  
Si el elemento no se encuentra, devuelve -1.  
Finalmente, muestra en pantalla la posición del elemento encontrado.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto23.py

## Punto24

Este programa solicita al usuario que escriba texto en Python.  
El bucle se repite hasta que el usuario escriba 'salir'.  
Si se ingresa cualquier otra cosa, el programa muestra en pantalla lo que se escribió.  
Cuando se escribe 'salir', el programa termina mostrando un mensaje final.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto24.py

## Punto25

Este programa define una función en Python para determinar si un número es primo.  
La función `es_primo` verifica si el número es menor que 2, en cuyo caso no es primo.  
Luego, comprueba si tiene divisores desde 2 hasta la raíz cuadrada del número.  
Si encuentra algún divisor, devuelve False; si no, devuelve True.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto25.py

## Punto27

Este programa recorre una lista de temperaturas en Python.  
Usa un bucle `for` y la instrucción `continue` para saltarse las temperaturas negativas o cero, mostrando únicamente las positivas.  
Cada temperatura positiva se imprime en pantalla seguida de "°C".  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto27.py

## Punto28

Este programa recorre una lista de números en Python.  
Si encuentra un cero, muestra un mensaje indicando que se omite la división por cero y continúa con el siguiente número.  
Para los demás números, calcula 10 dividido entre el número y muestra el resultado en pantalla.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto28.py

## Punto29

Este programa recorre una lista de datos en Python que puede contener valores numéricos y no numéricos.  
Usa un bucle `for` y `continue` para ignorar los valores no numéricos, sumando únicamente los valores válidos.  
Finalmente, muestra en pantalla la suma de los valores numéricos de la lista.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto29.py

## Punto30

Este programa recorre una lista de números en Python y suma sus valores.  
Si un número es múltiplo de 3, se omite utilizando `continue`.  
Si la suma acumulada supera un límite definido, el bucle se termina con `break`.  
Cada paso se muestra en pantalla para indicar los números añadidos, omitidos y cuando se supera el límite.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto30.py

## Punto31

Este programa recorre grupos y elementos en Python usando bucles `for` anidados.  
Cuando el elemento es 3, se omite con `continue` afectando solo al bucle interno.  
Se muestra en pantalla el número de grupo, los elementos recorridos, los elementos omitidos y un mensaje al finalizar cada grupo.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto31.py

## Punto32

Este programa recorre dos bucles anidados en Python para encontrar el primer producto de `i * j` mayor que 10.  
Cuando se encuentra, muestra el resultado y sale del bucle interno usando `break`.  
Luego, verifica la variable `encontrado` para salir también del bucle externo y finalizar el programa.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto32.py

## Punto33

Este programa valida contraseñas en Python según tres criterios:  1. Tener al menos 8 caracteres.  2. Contener al menos una letra mayúscula y una minúscula.  3. Contener al menos un número.  

Se define la función `validar_contraseña` para verificar estos requisitos.  
Luego se prueban varias contraseñas, mostrando en pantalla si son válidas o no.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto33.py

## Punto34

Este programa recorre una lista de transacciones en Python.  
Se procesan solo las transacciones completadas con montos válidos (mayores que cero).  
Se utiliza `continue` para saltar transacciones no completadas o con montos inválidos.  
Cada transacción procesada se muestra en pantalla y al final se indica el total procesado.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto34.py

## Punto35

Este programa recorre los números del 1 al 9 usando un bucle `for`.  
Para los números pares no realiza ninguna acción (`pass`).  
Para los números impares, imprime un mensaje indicando que se están procesando.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto35.py

## Punto36

Este programa recorre 100 iteraciones usando un bucle `for`.  
Si la variable `modo_debug` está en `False`, no se muestra ninguna información durante el procesamiento (`pass`).  
Si `modo_debug` está en `True`, se imprime cada iteración procesada.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto36.py

## Punto37

Este programa busca un número primo en una lista de números usando un bucle `for`.  
Al encontrar un número primo, se muestra en pantalla y se sale del bucle con `break`.  
Si no se encuentra ningún primo, se ejecuta el bloque `else` del bucle, mostrando un mensaje indicándolo.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto37.py

## Punto38

Este programa valida una lista de edades en Python.  
Se recorre cada edad verificando que sea un número entero y no negativo.  
Si se encuentra alguna edad inválida, se muestra un mensaje y se detiene la verificación.  
Si todas las edades son válidas, se indica en pantalla y la función retorna `True`.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto38.py

## Punto39

Este programa busca un usuario por nombre en una lista de usuarios en Python.  
Si el usuario existe, muestra su información.  
Si no existe, crea un nuevo perfil con nivel inicial 1 y lo agrega a la lista.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto39.py

## Punto40

Este programa analiza una lista de valores en Python y verifica si superan un umbral determinado.  
Si algún valor excede el umbral, se muestra una advertencia.  
Si todos los valores están dentro del rango normal, se indica que el análisis fue exitoso.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto40.py

## Punto41

Este programa calcula una aproximación de la raíz cuadrada de un número usando el método de Newton-Raphson.  
Se realiza un bucle iterativo que actualiza la aproximación hasta que sea suficientemente precisa o se alcance un número máximo de iteraciones.  
Cada iteración se muestra en pantalla, indicando el progreso de la aproximación.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto41.py

## Punto42

Este programa valida un formulario de datos en Python.  
Verifica que los campos requeridos (nombre, email, edad) existan y no estén vacíos,  
comprueba el formato del email y que la edad esté dentro de un rango permitido (18-120).  
También valida opcionalmente el teléfono, si se proporciona.  

El resultado indica si el formulario es válido o cuáles errores se encontraron.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto42.py

**FUNCIONES**

## Punto1

Este programa calcula el área de un rectángulo en Python.  
La función recibe como parámetros la base y la altura del rectángulo,  
realiza la multiplicación correspondiente y devuelve el resultado.  

Finalmente, el programa imprime el área calculada con un mensaje explicativo.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto1.py

## Punto2

Este programa contiene dos funciones en Python:
es_par(numero): Verifica si un número ingresado es par o impar.
celsius_a_fahrenheit(celsius): Convierte una temperatura de grados Celsius a grados Fahrenheit.
El resultado de cada función depende del valor que se le pase como argumento.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.
Ejecutar:
   python punto2.py

## Punto3

Este programa muestra cómo asignar una función a una variable en Python
y luego utilizar esa variable para llamar la función como si fuera la original.
En este caso se convierte la temperatura de 25°C a Fahrenheit
usando la función celsius_a_fahrenheit.
El resultado final se imprime en consola.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.
Ejecutar:
   python punto3.py

## Punto4

Este programa define una función llamada `calcular_descuento` que aplica un porcentaje de descuento a un precio dado.  
El porcentaje por defecto es 10%, pero puede modificarse al llamar la función.  
En el ejemplo se calcula el precio final de un producto que cuesta 100 con el descuento por defecto, obteniendo como resultado 90.0.  

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:  

   python punto4.py

## Punto5

Este programa muestra cómo se utilizan parámetros y argumentos en una función de Python.  
El parámetro `nombre` es usado para recibir un valor al llamar la función.  
En el ejemplo, al pasar el argumento `"Ana"`, la función imprime un saludo personalizado.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto5.py

## Punto6

Este programa calcula el precio final de un producto aplicando un impuesto.  
Se utilizan parámetros para recibir el `precio_base` y el `impuesto`, y se retorna el resultado del cálculo.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:
   python punto6.py

## Punto7

Este programa muestra cómo usar parámetros con valores predeterminados en Python.
La función saludar recibe un nombre y opcionalmente un mensaje.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.
Ejecutar:

   python punto7.py

## Punto8

Este programa muestra cómo usar argumentos posicionales y argumentos por nombre en Python.  
La función `dividir` recibe dos parámetros: `dividendo` y `divisor`, y retorna el resultado de la división.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:

   python punto8.py

## Punto9

Este programa muestra cómo usar argumentos por nombre en Python.
La función `crear_usuario` recibe datos de un usuario y devuelve un diccionario con su información.

## Cómo ejecutarlo

Abrir la terminal en la carpeta `src/iterativas`.  
Ejecutar:

   python punto9.py

## Punto10

Este programa muestra cómo usar parámetros variables en Python con `*args`.  
La función `sumar` recibe cualquier cantidad de números y devuelve la suma total.


### Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.  
Ejecutar:
   python punto10.py

## Punto11

Este programa muestra cómo usar parámetros variables con nombre en Python usando `**kwargs`.  
La función `mostrar_informacion` recibe cualquier cantidad de argumentos por nombre y los imprime en pantalla.


### Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.  
Ejecutar:
   python punto11.py

## Punto12

Este programa define una función `calcular_cuadrado` que recibe un número, calcula su cuadrado y devuelve el resultado.  
Luego se llama a la función con el valor `4` y se imprime el área resultante (16).

### Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.  
Ejecutar:
   python punto12.py

## Punto13

Este programa muestra cómo funciona una función en Python cuando no se utiliza `return`.  
La función `saludar` imprime un mensaje en pantalla, pero al no devolver nada explícitamente, su valor de retorno es `None`.

### Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.  
Ejecutar:
   python punto13.py

## Punto14

Este programa define una función `estadisticas` que recibe una lista de números y devuelve la suma, el promedio, el valor mínimo y el valor máximo. Posteriormente, esos resultados se desempaquetan en variables y se muestran en pantalla para observar cómo una función puede retornar múltiples valores en Python.

### Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.  
Ejecutar:
   python punto14.py

## Punto15
Este programa tiene una función que divide dos números. Si el número de abajo es cero, te avisa que no se puede.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto15.py

## Punto16
Este programa usa funciones que responden con True o False. La primera función revisa si alguien es mayor de edad, y la segunda revisa si un correo es válido. Luego, usa esas respuestas para decidir qué mensaje mostrar.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto16.py

## Punto17
Este programa tiene una función que toma un nombre y un apellido y los junta. El apellido lo pone en mayúsculas y el nombre lo pone con la primera letra en mayúscula. Esto sirve para organizar nombres de una forma específica.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto17.py

## Punto18
Este programa tiene una función para calcular el precio final de algo, incluyendo el IVA. La función ya tiene un valor de IVA por defecto, así que si no le das otro, usará ese. Esto hace que sea más fácil de usar.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto18.py

## Punto19
Este programa te muestra cómo hacer listas y diccionarios de forma más rápida. Usa una técnica llamada "comprensión de listas" para crear una lista de números pares y otra llamada "comprensión de diccionarios" para crear un diccionario de números al cuadrado. Es una forma más corta y elegante de escribir código.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto19.py

## Punto20
Este programa tiene una función que revisa una lista de números y solo te devuelve los que son positivos. Lo que es genial de esta función es que si por error le das algo que no es una lista, en lugar de fallar, te devuelve una lista vacía.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto20.py

## Punto21
Este programa tiene una función que solo calcula el promedio de unos números. El resultado se muestra fuera de la función. Esto es útil porque la función solo hace una cosa, y el programa principal decide qué hacer con el resultado.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto21.py

## Punto22
Este programa tiene una función que toma un número, por ejemplo, una puntuación, y te dice a qué calificación pertenece. También revisa si el número es válido, por si alguien pone un número muy alto o muy bajo.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto22.py

## Punto23
Este programa tiene una función muy simple que se encarga de sumar dos números. Su propósito es demostrar la función sumar y cómo un programa puede usarla para hacer una operación matemática básica.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto23.py

## Punto24
Este programa tiene una función que calcula el promedio de una lista de números. La función solo se encarga de hacer la cuenta, y luego se usa el resultado en el programa principal para mostrarlo en pantalla. Esto es una buena práctica porque la función se centra en una sola tarea, lo que hace el código más limpio.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto24.py

## Punto25
Este programa tiene una función que valida una dirección de correo electrónico. La función revisa que el texto tenga un @ y que el dominio (lo que va después del @) tenga un punto. Si le das algo que no es texto, te avisa con un error.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto25.py

## Punto26
Este programa tiene una función para convertir una temperatura de Fahrenheit a Celsius. Esto demuestra cómo puedes usar una función para hacer una tarea específica y luego usar el resultado en tu programa principal.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto26.py

## Punto27
Este programa tiene una función que toma una lista de números y te devuelve una lista nueva solo con los números que son pares.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto27.py

## Punto28
Este programa tiene una función que revisa si una persona es mayor de edad. Solo le das la edad, y te dice si es 18 años o más. Esto es útil para programas que necesitan verificar la edad, como para dar acceso a contenido.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto28.py

## Punto29
Este programa tiene una función que hace una división de forma segura. Antes de dividir, revisa si el número de abajo es cero. Si es así, no hace la operación para evitar un error. Si no, hace la división y te da el resultado.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto29.py

## Punto30
Este programa tiene una función para crear contraseñas al azar. Puedes decirle qué tan larga la quieres, pero si no le dices nada, hace una de 8 caracteres. Esto es útil para crear contraseñas seguras de forma rápida.Cómo ejecutarloDesde la terminal, ve a la carpeta src/funciones y escribe:   python punto30.py

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto30.py

## Punto31
Este programa tiene una función para crear contraseñas al azar. Si no le dices qué tan larga la quieres, hace una de 8 caracteres. Esto es útil para crear contraseñas seguras de forma rápida.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto31.py

## Punto32
Este programa te muestra la mejor forma de documentar tus funciones. Es mejor ser directo y conciso para que cualquiera pueda entender rápidamente lo que hace el código.

## Cómo ejecutarlo
Abrir la terminal en la carpeta `src/funciones`.
   python punto31.py